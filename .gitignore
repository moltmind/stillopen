# Secrets & credentials
.env
*.env
credentials*
keys.rtf
.mcp.json

# System files
.DS_Store

# Backend (deployed separately via Wrangler)
worker.js
wrangler.toml
.wrangler/

# Node
node_modules/
package-lock.json

# Personal / sensitive
*.docx
*.pptx
*.rtf
leads/

# AI tooling
.aidesigner/*
!.aidesigner/.gitkeep
.claude/
.agents/
backups/

# Local admin secret for wrangler-managed ADMIN_SECRET — do not commit
.admin-secret-local

# Internal / staging — never ship to the public site
drafts/
test/
tools/
competitor-brief.md

# Video assets live in Cole's local workflow, not the deployed site
video-ad-*/
video-promo-*/
hero-landscape-*/
